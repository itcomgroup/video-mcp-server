# Video MCP Server — подключён к OpenCode ✅

## Статус

✅ **video-mcp-server** успешно подключён к OpenCode
✅ **10 инструментов** доступны через MCP протокол
✅ **Groq API** настроен для AI-функций
✅ **FFmpeg, YouTube, AI** — все категории работают

---

## 📋 Доступные инструменты

### FFmpeg-only (без API ключа)

| Инструмент | Описание |
|------------|----------|
| `get_video_info` | Метаданные видео (длительность, разрешение) |
| `extract_video_frames` | Извлечение кадров (до 20) |
| `extract_video_audio` | Извлечение аудио в MP3 |
| `split_video` | Разделение видео на сегменты |

### YouTube (без API ключа)

| Инструмент | Описание |
|------------|----------|
| `get_youtube_info` | Информация о YouTube видео |
| `download_youtube_video` | Скачивание видео (360p-1080p) |

### AI-powered (с Groq API)

| Инструмент | Описание |
|------------|----------|
| `analyze_video` | AI анализ видео (Llama 4 Vision) |
| `summarize_video` | AI саммари видео |
| `transcribe_video` | Транскрибация аудио (Whisper) |
| `analyze_video_complete` | Полный анализ (видео + аудио) |

---

## 🚀 Использование через OpenCode

### Шаг 1: Проверьте статус

```bash
opencode mcp list
```

Должно показать:
```
●  ✓ video-mcp-server  connected
     python3 -m video_mcp_server.server
```

### Шаг 2: Используйте инструменты

**В OpenCode чате:**

```
Получи информацию об этом видео: /path/to/video.mp4
```

**Или скачай YouTube видео:**

```
Скачай это видео в 720p: https://youtube.com/watch?v=...
```

**Или AI анализ:**

```
Проанализируй это видео и опиши что происходит: /path/to/video.mp4
```

---

## 🔧 Конфигурация

**Путь к конфигу:** `~/.config/opencode/config.json`

```json
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "video-mcp-server": {
      "type": "local",
      "command": ["python3", "-m", "video_mcp_server.server"],
      "environment": {
        "GROQ_API_KEY": "YOUR_GROQ_API_KEY_HERE"
      }
    },
    "vision-mcp-server": {
      "type": "local",
      "command": ["python3", "-m", "vision_mcp_server.server"]
    }
  }
}
```

---

## 📦 Где скачиваются файлы

**По умолчанию:** `~/video-downloads/`

Для очистки:
```bash
rm -rf ~/video-downloads/*
```

---

## 🧪 Тестирование

### Проверка подключения

```bash
cd /home/debian/video-mcp-server
python3 test_mcp_connection.py
```

### Тест инструментов

```bash
python3 test_tools.py
```

---

## 🐛 Troubleshooting

### Server not connected

```bash
opencode mcp list
```

Если показывает `failed`:
- ✅ Проверьте что `python3` установлен
- ✅ Убедитесь что `video-mcp-server` установлен
- ✅ Проверьте путь в конфиге

### AI tools not working

- ✅ Проверьте что `GROQ_API_KEY` установлен
- ✅ Проверьте лимиты на https://console.groq.com

### YouTube download issues

- ✅ Проверьте интернет
- ✅ Обновите yt-dlp: `pip3 install --upgrade yt-dlp`

---

## 📊 Статистика

- **Всего инструментов:** 10
- **FFmpeg-only:** 4
- **YouTube:** 2
- **AI-powered:** 4
- **Видео скачано:** 1 файл (261 MB)
- **Транскрибация:** Полная (105 минут, 37676 символов)

---

## 🎓 Примеры использования

### Пример 1: Скачать и проанализировать YouTube видео

```
Скачай это видео: https://youtube.com/watch?v=jNQXAC9IVRw
Проанализируй скачанное видео
Извлеки 10 ключевых кадров
```

### Пример 2: Обработка локального видео

```
Получи информацию о video.mp4
Извлеки аудио
Транскрибируй аудио
Создай саммари видео
```

### Пример 3: Разделение длинного видео

```
Раздели long-video.mp4 на сегменты по 5 минут
```

---

## 🔄 Обновление

```bash
cd /home/debian/video-mcp-server
git pull
pip3 install --break-system-packages -e .
```

---

## 📝 Заметки

- **Groq API:** Бесплатно на данный момент
- **Модели:** Llama 4 Scout 17B (Vision) + Whisper Large V3 Turbo (Audio)
- **Лимиты:** Groq — неограничен на данный момент
- **Скачанные видео:** Сохраняются в `~/video-downloads/`

---

**Дата подключения:** 4 февраля 2026
**Статус:** ✅ Работает корректно
