# TODO: План установки локального Whisper на сервере

## Проблема
На текущем сервере недостаточно места для установки Whisper (только 128 MB свободно, нужно минимум 500 MB - 2 GB)

## Шаги для установки на сервере

### 1. Освободить место (минимум 2-3 GB)
```bash
# Удалить скачанные видео и аудио
rm -rf ~/video-downloads/*

# Очистить кэш pip
pip3 cache purge

# Очистить кэс huggingface/whisper (если есть)
rm -rf ~/.cache/huggingface
rm -rf ~/.cache/whisper

# Проверить свободное место
df -h ~
```

### 2. Установить зависимости
```bash
# Обновить pip
pip3 install --upgrade pip

# Установить FFmpeg (если не установлен)
sudo apt install ffmpeg

# Установить OpenAI Whisper
pip3 install openai-whisper
```

### 3. Скачать модель (выбрать размер)
```bash
# tiny - 39 MB (быстрее всего, но точность ниже)
whisper download tiny

# base - 74 MB (баланс скорости и точности) ← РЕКОМЕНДУЕТСЯ
whisper download base

# small - 244 MB (лучше точность)
whisper download small
```

### 4. Тестирование
```bash
# Транскрибация аудио
whisper audio.mp3 --model base --language ru --output_format txt

# Для длинного аудио разбить на части
# (как мы делали для Groq)
```

### 5. Интеграция в video-mcp-server
```python
# Добавить опцию локальной транскрибации
# в server.py

import whisper

class LocalTranscriber:
    def __init__(self, model_size="base"):
        self.model = whisper.load_model(model_size)
    
    def transcribe(self, audio_path, language="ru"):
        result = self.model.transcribe(
            audio_path, 
            language=language,
            verbose=False
        )
        return result["text"]
```

## Преимущества локального Whisper
- ✅ Полностью бесплатно
- ✅ Не зависит от API ключей
- ✅ Работает оффлайн
- ✅ Можно использовать любую модель (tiny/base/small/medium/large)

## Недостатки
- ⚠️ Требует много места на диске (500 MB - 3 GB)
- ⚠️ Медленнее на CPU (но можно использовать GPU)
- ⚠️ Для больших файлов нужно разбивать на части

## Приоритет
🔴 **ВЫСОКИЙ** — установить на следующей неделе

## Примечания
- Рекомендуется модель `base` (74 MB) — баланс скорости и точности
- Для критически важной транскрибации использовать `small` (244 MB)
- Транскрибация ~100 минут аудио на CPU: ~30-45 минут с моделью `base`
